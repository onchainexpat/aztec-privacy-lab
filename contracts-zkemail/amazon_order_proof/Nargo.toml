[package]
name = "amazon_order_proof"
type = "bin"
authors = [""]
compiler_version = ">=1.0.0"

[dependencies]
zkemail = { tag = "v2.0.0", git = "https://github.com/zkemail/zkemail.nr", directory = "lib" }
